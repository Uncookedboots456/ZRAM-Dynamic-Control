# ZRAM Dynamic Configuration
# Units: ZRAM_SIZE_GB (GB), BOOT_DELAY_SEC (seconds), SAFE_MEM_THRESHOLD_MB (MB)
# Only pure numbers are allowed for numeric values.

ZRAM_SIZE_GB=12
COMP_ALGORITHM=zstd
SWAPPINESS=100
WATERMARK_SCALE_FACTOR=150
BOOT_DELAY_SEC=60
SAFE_MEM_THRESHOLD_MB=1024